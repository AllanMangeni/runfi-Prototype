[project]
name = "runfi-prototype"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "C4", "T20"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
